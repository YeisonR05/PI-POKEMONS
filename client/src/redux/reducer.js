const inicialState = {
	pokemons: [],
	pokemonDetail: {},
	pokemonsTypes: []
}

const reducer = (state = inicialState, action) => {
	switch( action.type ) {

		default:
			return {...state}
	}
};

export default reducer;